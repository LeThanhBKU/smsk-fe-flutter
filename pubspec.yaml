name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.0.0+4  # Dùng cho mobile và desktop
  sqflite_common_ffi: ^2.0.0  # Dùng cho desktop
  path: ^1.8.0             # Thư viện để xử lý đường dẫn
  firebase_core: ^3.4.1  # Cập nhật phiên bản phù hợp với SDK Dart
  cloud_firestore: ^5.4.1
  firebase_core_web: ^2.18.0
  numberpicker: ^2.1.1
  shared_preferences: ^2.0.9  # Thư viện để lưu trữ trạng thái
  provider: ^6.0.5 
  geolocator: ^13.0.1
  http: ^1.2.2
  speech_to_text: ^7.0.0
  flutter_tts: ^4.0.2
  permission_handler: ^11.3.1
  speech_to_text_platform_interface: ^2.2.0
  cupertino_icons: ^1.0.2
  esp_smartconfig: ^2.0.4
  network_info_plus: ^6.1.1
  web_socket_channel: ^3.0.1
  wifi_info_flutter: ^2.0.2
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0    # Dùng để kiểm tra code

flutter:
  uses-material-design: true
